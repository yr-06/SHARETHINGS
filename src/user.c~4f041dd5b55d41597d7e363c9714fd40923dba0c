#include <stdio.h>
#include "../include/user.h"
#include "../include/struct.h"

void get_user(FILE *g){
    string s;
    string c;
    if (g=null){
        init-mtp(g);
    }
    printf("Veuillez entrer votre identifiant :\n");
    scanf("%s",&s);
    fscanf(f,"%s",&c);
    if (s=c){
        printf("Bienvenue dans le menu administrateur\n");
        printf("Que voulez-vous faire ?\n);
        printf(
    
}
void init_user(FILE*g){}/*initialiser l'identifiant/ mot de passe (id:8 max; mtp:8 max); verifier que l'id et le mot de passe ne sont pas déjà présents ds le fichier*/
void rech(FILE *g){} /*rechercher une ressource par type, elle va demander à l'utilisateur de saisir le type de ressources recherchés*/
void consult(FILE *g){}
void gest(FILE *g){}
